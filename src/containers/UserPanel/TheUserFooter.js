import React from "react";
import { CFooter } from "@coreui/react";

const TheUserFooter = () => {
  return (
    <CFooter fixed={false}>
      <div></div>
    </CFooter>
  );
};

export default React.memo(TheUserFooter);
